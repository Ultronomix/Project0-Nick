GitHub create new repository
1. git init
2. git add Readme.md
3. git commit -m "first commit"
4. git branch -M main
5. git remote add origin(name) url
6. git push -u origin(name) main

push an existing repository
1. git remote add origin(name) url
2. git branch -M main
3. git push -u origin(name) main
